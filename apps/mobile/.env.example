# 선택 기능(LLM 후편집·요약)을 쓸 때만 필요하다.
# 이 값은 앱 번들에 들어가지 않는다. 앱 설정 화면에서 입력하면
# expo-secure-store(iOS Keychain / Android Keystore)에 저장된다.
# 여기 적는 것은 개발 중 시뮬레이터 테스트용이다.
EXPO_PUBLIC_ENABLE_CLOUD_FEATURES=false
