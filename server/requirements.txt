# NSR MCP 서버. 파이썬 3.11 이상.
mcp>=2.1,<3
uvicorn>=0.30

# QR 로 폰 잇기 (python -m nsr_server.pair). 없어도 서버는 돌고, 주소만 나온다.
qrcode>=7

# 시험용 (README 의 pytest 안내가 이걸 쓴다)
pytest>=8
httpx>=0.27   # 테스트 클라이언트가 쓴다 (서버 실행에는 필요 없다)
