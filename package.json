{
  "name": "nsr-project",
  "private": true,
  "version": "0.1.0",
  "description": "NSR — 신규간호사 적응 지원 앱 (근무 녹음 · 전사 교정 · 학습카드 · 태움 스코어 · 듀티표)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @nsr/core",
    "typecheck": "npm run typecheck --workspace @nsr/core",
    "build": "npm run build --workspace @nsr/core",
    "check": "npm run typecheck && npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
