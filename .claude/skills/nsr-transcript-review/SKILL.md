---
name: nsr-transcript-review
description: NSR 근무 녹음의 휘스퍼 전사본을 검토·교정할 때 반드시 먼저 읽는 절차와, 실제 교정 세션에서 확정된 판별 기준의 기록. 사용자가 전사본을 붙여넣거나 data/transcripts 에 넣었다고 하거나 "교정해줘", "전사 검토", "이 단어 뭐야", "휘스퍼가 이상하게 적었어" 라고 하면 스킬 이름 없이도 적용한다. 오류를 A 반복 · B 임상 어휘 부재 · C 순수 한글 오독으로 가르고, 앞뒤 문맥으로 더블체크하고, 판독 불가는 파일명·시각·문장과 함께 묻는다. 판별 기준과 예시는 머리로 짓지 않고 references/ 에 실제 세션에서 확정된 것만 쌓는다. Claude API 스킬로도 같은 폴더가 올라간다.
---

# NSR 전사본 검토

이 스킬은 **실제 전사본을 교정하면서 채워 가는 기록**이다. 사용자가 휘스퍼 전사본을 보내면
Claude 가 교정하고, 그때 확정된 판단만 `references/` 에 남긴다. 다음 세션과 API 스킬은
그 기록을 기준으로 같은 판단을 한다. 여기 없는 기준을 즉석에서 지어내 확정하지 않는다.

## 지금 상태

- 채워진 교정 세션: **2회** (2026-09-02 규칙 검토, 2026-09-03 문맥 교정 · 녹음 8개 6,391문장).
- 확정된 규칙 73건 · 표제어 55개 → `references/confirmed-rules.md`. 이 표에 있는 것은 묻지 않고 고친다.
- 표에 없는 것: 문맥으로 후보를 세우되, 확신이 서지 않으면 **묻는 쪽**으로 기운다.

## 사용자가 정한 틀 (바뀌지 않는 부분)

1. 오류를 세 갈래로 가른다 — **A 반복**(같은 말이 되풀이됨), **B 임상 어휘 부재**(모델이 모르는 용어를 다른 말로 적음), **C 순수 한글 오독**(평범한 말을 잘못 적음). 어느 갈래인지 먼저 정한다.
2. **문맥으로 더블체크**한다. 문장 하나가 아니라 앞뒤 대화를 보고, 그 자리에 맞는 어휘인지 본다.
3. 대화 문맥에서 **어떤 단어가 적절한지 추론**해 후보를 세운다.
4. **판독 불가**는 추측해서 확정하지 않고, 파일명·시각·문장을 붙여 사용자에게 묻는다.
5. 녹음본 여러 개를 넣어 **서로 교정**하며 확정을 쌓는다. 같은 오인식은 두 번 묻지 않는다.
6. 충분히 쌓이면 이 스킬 파일을 점검해 각 AI(Claude Code · Claude API)에 보낼 판을 만든다.

## 저장소 규칙에서 오는 대전제

- `rawText`(원문)는 보존한다. 교정은 별도 기록이다.
- 화자가 실제로 한 말(은어 "폴리")은 고치지 않는다. 음성인식이 틀린 것만 고친다.
- 환자 실명·등록번호·병실은 산출물과 질문에 옮기지 않는다. 환자A·환자B 로 쓴다.
- 자세한 것: `CLAUDE.md` 절대 규칙, `nsr-privacy` 스킬.

## 절차

산출물은 **사람이 읽을 수 있는 교정본**이다. 사전 치환만 한 판은 읽히지 않는다는 것이 2026-09-03 에
확인됐다 — 도구는 보조, 문맥 교정이 본체다.

1. **결정적 1차 검토를 돌린다.** 사람이 읽기 전에 규칙이 잡을 수 있는 것을 걷어낸다.
   ```bash
   node tools/review-transcript.mjs data/transcripts/<파일>
   ```
   `data/reviews/<파일>.review.md` 에 자동 적용(사전에 이미 있는 오인식·약어 읽기·이전 확정) ·
   확인 필요 · 질문 후보가 나온다. 도구는 **후보만** 모은다. 갈래 판정은 하지 않는다.
2. **문맥 교정을 직접 한다.** 규칙은 `references/context-correction.md`. 앞뒤 20줄을 읽고 낱말을
   되돌려 문단으로 묶은 읽는 판(`data/reviews/<파일>.readable.md`)과 고친 목록(`.items.jsonl`)을 만든다.
   표기는 높음 조용히 / 중간 `(?)` / 낮음 `[?원문]`. 반복은 접는다. 긴 파일은 녹음 단위로 나눠 병렬로.
3. **후보마다 갈래를 정한다.** 기준은 `references/decisions.md`. 거기 없는 경우는 문맥으로
   후보를 세우고 확신 등급을 낮게 매긴다.
4. **문맥 더블체크.** 앞뒤 2~3문장, 지금 어느 환자·어떤 처치 얘기인지, 제안한 말을 넣어 문장이
   되는지, 옆의 숫자·단위와 맞는지 본다.
5. **판정표를 쓴다** (아래 형식). 확신 높음/중간/낮음을 반드시 적는다. items 를 모아 B 갈래 빈도표를
   만들고 2번 이상 나온 것은 `data/corrections/proposed.jsonl` 에 올린다.
6. **낮음은 질문으로 넘긴다** (아래 형식). 한 번에 15개 이내, 투약 > 처치 > 검사 > 업무 순.
7. **답을 받으면 기록한다** (아래 "확정 후").

## 판정표 형식

| # | 파일 | 시각 | 원문 | 갈래 | 제안 | 근거 (문맥) | 확신 |
| --- | --- | --- | --- | --- | --- | --- | --- |

표 뒤에 한 줄: 자동 적용 n건 / 확인 요청 n건 / 질문 n건.

## 질문 형식

파일명과 시각이 없는 질문은 내지 않는다 — 사용자가 녹음의 그 지점을 찾아 들어야 한다.

```
[질문 1] 파일: 2026-08-24_D_01.json · 시각: 00:12:34
문장: "… [?원문 표기] …"   (환자 정보는 가린 채로)
후보: ① … ② … ③ 다른 말
왜 못 정하나: …
```

## 확정 후

사용자 답을 받으면 그 자리에서:

1. `references/log.md` 에 세션 기록 한 줄 — 파일·시각·원문 표기·확정 표기·갈래·근거. 문장 원문은 가린 형태로만.
2. 판별 **기준**으로 일반화할 수 있는 것(같은 종류가 두 번 이상 나왔거나, 사용자가 "앞으로 이렇게" 라고 한 것)은 `references/decisions.md` 로 올린다.
3. `data/corrections/confirmed.jsonl` 에 규칙 한 줄 (형식은 `data/README.md`). 다음 검토부터 도구가 먼저 적용한다.
4. B 갈래가 두 번 이상 확정되면 `packages/core/src/lexicon/misheard.ts` 에 올리고 회귀 테스트를 함께 넣는다 (`test-driven-development`). C 갈래는 사전에 넣지 않는다. A 갈래는 규칙으로 만들지 않는다.
5. 내 판정을 사용자가 뒤집은 경우는 `references/decisions.md` 의 "뒤집힌 판정" 에 적는다. 다음 세션이 같은 실수를 안 하게.

## API 스킬로 쓰일 때

`tools/upload-skill.mjs` 로 이 폴더가 그대로 Claude API 에 올라간다. 그때 모델은 **저장소도 도구도
없이 이 문서와 `references/` 만** 읽는다. 그러니 이렇게 한다.

- 절차 1(검토 도구)은 건너뛴다. 대신 `references/confirmed-rules.md` 를 규칙표로 삼아 직접 적용한다.
  그 표에 있는 말은 확신 "높음" 으로 조용히 고친다 — 다시 묻지 않는다.
- 출력은 **읽는 판**(`references/context-correction.md` 의 출력 A 형식) + 못 정한 것 질문이다.
  판정표만 내지 않는다. 사람이 읽을 수 있는 판이 산출물이다.
- 고친 목록(출력 B)은 JSON 줄로 함께 낸다. 사용자가 그것으로 규칙을 늘린다.
- 입력의 `[이름]`·`[생년월일]` 가림 표시는 그대로 둔다. 가려지지 않은 이름은 `[이름]` 으로 바꾼다.
- 파일에 쓸 수 없으므로 답변 본문에 낸다. 길면 녹음 단위로 나눠 이어서 낸다.

**규칙을 새로 만들지 않는다.** 확정은 사용자만 한다. 표에 없는 것은 `(?)`·`[?말]` 로 두고 묻는다.

## 참고

- `references/confirmed-rules.md` — **확정 규칙표.** `confirmed.jsonl` 에서 자동 생성된다
  (`node tools/sync-skill-rules.mjs`). 손으로 고치지 않는다.
- `references/decisions.md` — 판별 기준과 뒤집힌 판정 (왜 그렇게 정했는지)
- `references/context-correction.md` — 읽는 판을 만드는 규칙 (표기·문단·반복·출력 형식)
- `references/log.md` — 세션별 교정 기록
- `docs/07-transcript-review-workflow.md` — 누가 언제 무엇을 하는지
- `docs/02-transcription-pipeline.md` — 휘스퍼가 왜 이렇게 틀리는지
